<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/bootstrap-icons-1.10.3/bootstrap-icons.css" />
    <link rel="stylesheet" href="/style/style.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="container w-100 text-center mt-2 py-5">
      <form
        class="mt-5 d-flex justify-content-center needs-validation"
        action="/register"
        method="post"
        novalidate
      >
        <div class="w-50">
          <div class="p-4 card d-flex">
            <h3 class="mb-5 text-center">تسجيل حساب جديد</h3>
            <%if (messages) {%> <%for(let message of messages) {%>
            <div class="alert alert-danger" role="alert">
              <h5><%=message%></h5>
            </div>
            <%}%> <%}%>
            <div class="form-group form-floating mb-3">
              <input
                type="text"
                class="form-control"
                id="username"
                name="username"
                placeholder="اسم المستخدم"
                required
              />
              <label for="username">اسم المستخدم</label>
              <div class="invalid-feedback text-start">اكتب اسم المستخدم</div>
            </div>
            <div class="form-group form-floating mb-3">
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                placeholder="كلمة المرور"
                required
                onselectstart="return false"
                onpaste="return false"
                onCopy="return false"
                onCut="return false"
                onDrag="return false"
                onDrop="return false"
                autocomplete="off"
              />
              <label for="password">كلمة المرور</label>
              <div class="invalid-feedback text-start">اكتب كلمة المرور</div>
            </div>

            <div class="form-group form-floating mb-3">
              <input
                type="password"
                class="form-control"
                id="confirm"
                name="confirm"
                placeholder="تأكيد كلمة المرور"
                required
                onselectstart="return false"
                onpaste="return false"
                onCopy="return false"
                onCut="return false"
                onDrag="return false"
                onDrop="return false"
                autocomplete="off"
              />
              <label for="confirm">تأكيد كلمة المرور</label>
              <div class="invalid-feedback text-start">
                اكتب تأكيد كلمة المرور
              </div>
            </div>
            <div class="form-check mb-3 text-start">
              <input
                class="form-check-input"
                type="checkbox"
                value=""
                id="readWrite"
                name="readWrite"
              />
              <label class="form-check-label" for="toggleConfirm">
                قراءة وتعديل
              </label>
            </div>

            <button type="submit" class="btn btn-primary">إنشاء الحساب</button>
            <div class="mt-3">
              <a href="/login" class="text-decoration-none btn btn-link">
                اذا كان لديك حساب يمكنك تسجيل الدخول
              </a>
            </div>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
