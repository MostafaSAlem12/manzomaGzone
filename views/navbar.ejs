<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"
      ><img src="/images/logospin.gif" width="60"
    /></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li
          class="nav-item <%= title === 'home' ? 'border-bottom border-info border-5' : '' %>"
        >
          <a class="nav-link" href="/">الصفحة الرئيسية</a>
        </li>
        <li
          class="nav-item <%= title === 'show' ? 'border-bottom border-info border-5' : '' %>"
        >
          <a class="nav-link" href="/show">عرض البيانات</a>
        </li>
        <li
          class="nav-item <%= title === 'charts' ? 'border-bottom border-info border-5' : '' %>"
        >
          <a class="nav-link" href="/charts">التحليل البياني</a>
        </li>
      </ul>

      <form class="d-flex input-group w-auto" action="/logout" method="POST">
        <div class="m-1 p-2 text-muted">
          <%if (username === 'youssef') {%>
          <h5>يوسف</h5>
          <%}%>
        </div>
        <button
          class="btn btn-outline-danger my-2 my-sm-0 ml-auto pt-1"
          type="submit"
        >
          تسجيل الخروج
        </button>
      </form>
    </div>
  </div>
</nav>
